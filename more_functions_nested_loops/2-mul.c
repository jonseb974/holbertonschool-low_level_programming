#include <stdio.h>
#include <ctype.h>
/**
*mul-return sum of multiplication of two int.
*@a: an integer
*@b: an integer
*@sum: sum of multiplication of two integers.
*Return: return 0.
*/

int mul(int a, int b)
{
	 mul = a * b;

	printf("%d", mul);
	return (0);
}
