#include <stdio.h>
#include <ctype.h>

/*
*_isupper - check  if letter is upper
*
*Return: 1 if upper, else 0.
*/

int _isupper(int c)
{
	if (_isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
