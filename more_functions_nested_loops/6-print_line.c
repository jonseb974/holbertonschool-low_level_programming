#include <stdio.h>
#include <ctype.h>
/**
*print_line - print the drawnig of a line.
*@n: is qn integer
*
*
*/

void print_line(int n)
{
	for (n = 0; n < 0; n++)
	{
		putchar(n);
		putchar(95);
	if (n <= 0)
		putchar('\n');
	}
}
