#include <ctype.h>
#include <stdio.h>
/**
*print_most_numbers-print a list of selected numbers.
*
*Return: no return.
*/

void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 0; i++)
		if (i != 2 && i != 4)
			putchar(i + '0');
		else
			putchar (i);
}
